CANVA_FRONTEND_PORT=8080
CANVA_BACKEND_PORT=3001
# CanvaCodeCraft's own deployment (no separate app backend server needed for
# this prototype — the frontend calls CanvaCodeCraft's /api/canva-app/designs
# endpoint directly). Use your deployed URL, not localhost, once submitted.
CANVA_BACKEND_HOST=http://localhost:5000
# Must match CANVA_APP_SHARED_SECRET in the CanvaCodeCraft deployment's own
# environment — see CLAUDE.md's Canva App section.
CANVA_APP_SHARED_SECRET=# TODO: generate a random secret and set it on both sides
CANVA_APP_ID=# TODO: Add your app's ID here to configure your backend for JWT verification
CANVA_APP_ORIGIN=# TODO: Add your app's origin here from the "Developer Portal -> Settings -> Security" to enable HMR
CANVA_HMR_ENABLED=FALSE # TODO: set to TRUE to enable HMR
